server:
  port: 8111
  servlet:
    context-path: /post


spring:
  application:
    name: POST
  datasource:
    url: "jdbc:mysql://localhost:3306/social_post"
    username: root
    password: ngoquang178
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092,localhost:9093,localhost:9094
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        "[spring.json.add.type.headers]": false
    properties:
      enable.idempotence: true
      acks: all
      retries: 5
      request.timeout.ms: 20000
      delivery.timeout.ms: 30000
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest

jwt:
  signerKey: "1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij"

kafka:
  topic: Post_Topic
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Địa chỉ của Eureka Server
    register-with-eureka: true # Tùy chọn, nếu bạn muốn ứng dụng cũng đăng ký
    fetch-registry: true # Bắt buộc để lấy thông tin từ Eureka
    enabled: true
  instance:
    hostname: localhost
#    prefer-ip-address: true # Sử dụng IP thay vì hostname


logging:
  level:
    org.springframework.web.socket: DEBUG
    org.springframework.messaging: DEBUG
